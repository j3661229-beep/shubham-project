{
  "name": "smart-supply-chain-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-frontend": "cd frontend && npm install",
    "install-backend": "cd backend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "build": "npm run vercel-build",
    "vercel-build": "cd frontend && npm install && npm run build && cd .. && rm -rf public && mv frontend/dist public"
  }
}
